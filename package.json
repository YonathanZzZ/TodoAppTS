{
  "name": "todoapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx ts-node -T server/src/server.ts",
    "heroku-postbuild": "cd client && npm install && npm run build && cd ../server && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/jwt-decode": "^3.1.0"
  }
}
